[build]
builder = "nixpacks"
buildCommand = "npm install && npx tsc"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Chromium needs these system packages for Remotion rendering
[build.nixpacksPlan.phases.setup]
nixPkgs = [
  "chromium",
  "ffmpeg",
  "nss",
  "atk",
  "cups",
  "libdrm",
  "gtk3",
  "mesa",
  "alsa-lib",
  "xorg.libXcomposite",
  "xorg.libXdamage",
  "xorg.libXrandr",
  "xorg.libXcursor"
]
